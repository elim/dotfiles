case $(uname) in
  FreeBSD|Darwin)
    alias ls='ls -wGF'
    ;;
  Linux)
    alias ls='ls -F --color=auto'
    ;;
esac

[[ $(basename ${SHELL}) = 'zsh' ]] && alias \\ls=ls

alias la='ls -AFH'
alias ll='la -l'

# Local Variables:
# mode: sh
# End:
