{
  (( $+commands[tmux] )) || return
  (( ${UID} == 0 ))      && return
  [[ -n ${TMUX} ]]       && return

  exec tmux new-session -A -s main \; \
       set-option set-titles-string main
}

# Local Variables:
# mode: sh
# sh-shell: zsh
# End:
